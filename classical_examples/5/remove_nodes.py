class linked:
	def __init__(self, elem):
		self.elem = elem
		self.next = None

def remove_nodes(linked_list, elem):
	
	return linked_list

a = linked('a')
b = linked('b')
a.next = b
c = linked('c')
b.next = c
new_list = remove_nodes(a)
